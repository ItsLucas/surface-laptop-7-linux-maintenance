**2026-09-14最终补记：SPI触摸屏已由用户实际验证并设为默认，iptsd3.1.0+sl7.2-1正式安装。今天的最终状态、完整补丁顺序、遗留问题见[今日最终状态](今日最终状态.zh-CN.md)。以下记录保留之前各阶段事实，旧的“等待启动/未安装”不代表当前状态。**

**目前结果：Wi-Fi 已联网，触控板完成指尖与拇指侧面校准，蓝牙鼠标可用，Secure Boot 始终保持开启。**

本记录结合当前系统、APT/安装器日志、最早的 `~/sl7-kit`、蓝牙修复记录和本次工作目录。能确认文件和时间的修改都列入；没有完整审计日志的手动操作不作推断。同日后来安装的开发工具也有单独记录，避免把它们混同于驱动修复。

**安装与原始环境**

- 安装介质为 Ubuntu 26.10 `Stonking Stingray` arm64 Daily（20260902），9 月 14 日安装完成。当前仍是 development branch。
- 安装器配置了 Qualcomm X1E 支持包 `hwe-qcom-x1e-meta`、`ubuntu-x1e-settings`、PD mapper，以及 Microsoft/Canonical 签名的 shim、ARM64 GRUB。
- BIOS 使用用户要求的 Microsoft & Third-Party CA 模式。原系统参数包含 `clk_ignore_unused pd_ignore_unused cma=128M efi=noruntime`；这些平台参数在组合内核中保留。
- APT 10:19 的升级安装了官方 `7.2.0-5-generic`，并升级约 303 个包；旧 `7.0.0-14-generic` 仍保留。安装器早期日志的时区与桌面会话不同，因此没有把 02:10 记录机械解释为当地操作时间。
- 已安装的内核跟踪入口实际是 `linux-generic-hwe-26.04`、`linux-image-generic-hwe-26.04`、`linux-headers-generic-hwe-26.04`，版本均为 `7.2.0-5.5`。普通 `linux-generic` 没有安装；不需要凭名字另换一套元包。

**Windows 固件提取与初期固件修正（约 10:35–11:33）**

安装 `qcom-firmware-extract=20`，随后安装本机生成的 `qcom-x1e-firmware-extracted-x1e80100-microsoft-romulus=20260914`。提取的 ADSP、CDSP、GPU 及相关描述文件位于 `/usr/lib/firmware/updates/qcom/x1e80100/microsoft/Romulus/`，约 25 MB。

早期离线工具包 `~/sl7-kit` 做了设备与 USB 插拔诊断，并实施两项固件修改：

1. 为 GPU 固件建立上一级路径 `.../microsoft/qcdxkmsuc8380.mbn -> Romulus/qcdxkmsuc8380.mbn`。
2. 在原 Wi-Fi `board-2.bin` 中，为 `subsystem-device=3378,qmi-board-id=255` 的板级数据添加本机 `1107/255` 别名。原 66 个名称及其数据保持不变，新增一个名称，结果共 67 个。修改后 SHA256 为 `30ced6b4ba1968866ddf15c9d128860a9c0a44f430a41f278d9daa1a44e194b7`。

覆盖文件写入 `firmware/updates`，发行版原始板级文件保留。早期另安装了 `/etc/initramfs-tools/hooks/sl7-kit-firmware`。后续确认系统实际使用 dracut，因此当前组合内核由 dracut 的 Ubuntu X1E 集成和显式 `--include` 带入固件；不能把这个旧 hook 当作 dracut 会自动执行的脚本。

板级错误消失后，Wi-Fi 仍报告 hard blocked，说明仍需处理独立的 rfkill 问题。最初的 USB/GPU 诊断和路径修复已记录，但不能据此宣称显示、摄像头、音频等所有功能都已验收。

**蓝牙地址和鼠标重连（约 13:36–13:38）**

内置 WCN7850 曾因缺少 public address 而处于 Unconfigured 状态。安装的 `/usr/local/sbin/sl7-bt-address` 从 Surface 的 `MacAddressEmulationAddress` UEFI 变量读取地址，减 1 得到蓝牙地址，仅对未配置的内置 UART 控制器设置地址。

配套文件包括 `sl7-bt-address.service`、`80-sl7-bt-address.rules` 和 `bluetooth.service.d/10-sl7-address.conf`。蓝牙恢复后，增加 `sl7-mouse-connect.service` 及脚本，对已配对、已信任的 BT5.2 鼠标进行有时限的开机重连；`/etc/bluetooth/main.conf` 明确设定 `AutoEnable=true`，旧配置有备份。此前会话中这只鼠标可用；最终检查发现本次启动的自动重连单元为 failed/exit 1，说明自动重连尚不能标记通过，后续用户已澄清当时主动关机，并要求清理，处理结果见文末。蓝牙地址服务本次执行成功。归档没有读取或保存配对密钥。

**Wi-Fi 与触控板定位**

- Wi-Fi：PCI WCN7850 hw2.0 已加载固件并创建接口，但 `rfkill` 为 hard=1。`rfkill unblock` 只能解除软阻断，无法解决这个信号问题。
- 触控板：官方设备树没有启用对应 QSPI 控制器及 HID 节点，内核缺少配套 HID-over-SPI 驱动，用户态也没有 iptsd。单独调整桌面设置无法补齐这条链。
- 7.2 中 Wi-Fi 7 拆分成 `ath12k_wifi7`，但 rfkill 配置仍在共享 `ath12k/core.c`。
- 官方内核是签名的 Stubble PE，内含压缩内核、设备树和机器匹配信息。只改裸 DTB 无法等价修改实际启动镜像。

**MOK 与 EFI runtime 试验**

最初只构建、签名了 Wi-Fi 模块，并登记模块用途 MOK `SL7 WiFi Local Module Signing 2026`。但官方内核在 `efi=noruntime` 下没有导入该 MOK；无操作测试模块被 lockdown 拒绝，因此没有强行切换无线驱动。

获准进行一次 `efi=runtime` 试启动后，系统能启动，但原生 EFI `GetVariable` 仍返回 `EFI_UNSUPPORTED`，内核平台密钥环没有解决导入问题。Qualcomm QSEE 的变量访问路径则能让 mokutil 正常工作。最终恢复原有 `efi=noruntime`，移除了临时 EFI runtime 测试项。

用户要求把触控板一并修复，于是改为基于精确 Ubuntu 源码构建组合内核，并把模块公钥编入内核。新建并登记允许启动镜像签名的 `SL7 Local Kernel Signing 2026` 证书；旧模块证书有 module-only 用途限制，不能代替它。没有关闭 Secure Boot、shim 校验或内核 lockdown。

**组合内核与 iptsd（约 14:22–15:56）**

基线是 `linux-source-7.2.0=7.2.0-5.5`，输出独立版本 `7.2.0-5-sl7`，不覆盖官方内核及模块目录。

- 移植两个内核补丁：限定 Romulus13/WCN7850 的 rfkill workaround；配套的 GENI QSPI、GPI DMA、HID-over-SPI v3 和 Romulus13 触控板设备树。
- 保持 Ubuntu 完整配置，新增 `CONFIG_SPI_HID=m`，改变模块签名证书路径；保留 Rust 配置，安装 Rust 1.95 与匹配构建工具。大量驱动输出来自完整配套模块构建及最终链接；`nice -n10` 让构建以较低调度优先级运行。
- 共生成并签名 8,643 个模块；验证 358,295 项导入符号 CRC，对 7 个关键模块实际验签。kmod 对 Rust 扩展版本表的 101 条误报已通过按内核规则解析扩展表解释。
- 内层 EFI 内核和外层 Stubble 镜像分别签名；校验嵌入设备树、内核字节和本机 HWID。使用现有 dracut 生成 initrd，保持已签名模块字节不变。
- 从 `alex-lentz/iptsd` 的固定提交构建 `iptsd=3.1.0+sl7.1`，包含实体点击处理、去抖、触点跟踪相关修正、systemd/udev 集成及恢复钩子。没有使用缺少服务/规则的预编译包。
- 安装 `linux-image-7.2.0-5-sl7=7.2.0-5.5+sl7.1` 和 iptsd；完成新 MOK 登记、单次试启动。确认 Secure Boot enabled、lockdown integrity、内建模块公钥加载、Wi-Fi hard block 清除、触控板已枚举并运行 iptsd。

**触控板校准（约 16:04–16:13）**

第一轮采集 7,794 个普通手指样本，指针抖动显著改善；但拇指侧面漏识别。第二轮补采 2,567 个样本，发现拇指触点尺寸约 1.85 cm、长宽比约 2.07，超过第一轮限制。合并两次推荐范围后，最终配置为：

```ini
[Contacts]
SizeMin = 0.800
SizeMax = 2.000
AspectMin = 1.000
AspectMax = 2.100
```

文件位于 `/etc/iptsd.d/91-calibration-045E-0C77.conf`，仅匹配 `045E:0C77`。用户确认指尖、拇指侧面和双指操作全部正常。配置独立于内核版本，日后不要被包升级覆盖。

**Wi-Fi 地址固定与正式默认启动（约 16:09）**

原驱动给出 `00:03:7f:12:8e:cd`，连 `ethtool -P` 也把它当作永久地址；这不代表真实出厂地址。按 [sl7-mac 的 UEFI 地址家族方法](https://github.com/valeronm/sl7-mac)，本机 Wi-Fi 推导值为 **`78:86:2e:5e:ec:47`**。蓝牙现有地址与同一地址家族的减 1 值相符；没有另行从本机 Windows 注册表核对 Wi-Fi 地址。

安装 Wi-Fi 专用 helper 和 `sl7-wifi-mac.service`，每次启动读 UEFI、生成限定内置网卡的 `.link` 和 NetworkManager 默认克隆地址。保留接口名 `wlP4p1s0`。本阶段不改蓝牙、固件变量或网络认证内容。

用户批准后，新内核正式设为默认，GRUB 显示 5 秒菜单，官方内核仍可选择。当前已确认 Wi-Fi 状态为 connected、实际地址正确、连接采用设备默认 MAC 策略，并具有 Wi-Fi 默认路由。用户也确认通过 Wi-Fi 联网。未记录 SSID、认证凭据，也不从“已联网”推断企业网络的具体认证策略。

**开发工具及同日其他安装**

驱动工作安装了 make、pahole、ELF/DWARF/OpenSSL 开发库、flex/bison/gawk、Rust 1.95、bindgen 0.72.1、Clang/LLVM 21、GCC/G++ 15、Meson/Ninja/CMake、iptsd 的 C++ 与 systemd 依赖。

APT 还记录了 htop/lm-sensors、git/cargo/curl，以及 16:16 之后的 build-essential、ripgrep/ffmpeg、Python 开发包、libffi、Xvfb、字体和图形库依赖。这些安装确实发生，但现有记录不足以归因于本次驱动修复；完整包名和版本见 `inventory/apt-transactions-20260914.json` 与 `installed-packages.tsv`。没有为本次归档修改或移除它们。

**仍需后续观察**

下一次正常重启核对默认内核与 Wi-Fi 地址自动应用；另行验证休眠/恢复、长期无线稳定性。摄像头、音频、触摸屏和完整 GPU/USB 回归不在本次已通过的验收范围。签名私钥仍只在 root 专用目录，已完成登记的一次性口令文件已删除。

**用户后续澄清与清理：** 鼠标此前由用户主动关机，等待超时符合这一情况，不能据此认定蓝牙故障。用户不再需要鼠标自动重连，已于本次归档中清理 `sl7-mouse-connect.service` 和对应脚本，保留正常蓝牙功能。历史文件移至 `history/retired-mouse-autoconnect/`，不在当前系统配置快照内。

**后续触摸屏试验（16:41）：** 用户交接了 9 月 7 日的 Romulus13 HID-over-I2C 补丁及已签测试镜像。复核确认只有嵌入 DT 改变，内核与 HWID 不变，内外签名有效，驱动及 initrd 支持齐备。用户批准后已安装独立 `sl7-touchscreen-test` 项并设置下一次启动，默认仍为正常组合内核；尚未重启，触摸屏尚未实测。实验文件位于 `experimental/touchscreen/`，未加入已验证的 kernel series。

**触摸屏首次启动结果（16:44后）：** 进入正确测试镜像，触控板校准已加载、Wi-Fi正确地址联网、Secure Boot有效，但8-0034触摸屏没有应答。限定重试与复位释放等待50/200/800ms均失败，临时诊断状态已恢复。用户确认此前Windows/UEFI正常，因此准备读取本机真实PnP/ACPI资源；尚未制作新的内核或试验镜像。


# 今日最终状态（2026-09-14）

Surface Laptop 7 13.8 英寸、X1E、Romulus13，Ubuntu 26.10。今天的系统工作已收尾，未额外重启。

| 项目 | 最终状态 |
|---|---|
| 内核 | Ubuntu 7.2.0-5.5 源码上的7.2.0-5-sl7；模块和启动镜像保持签名 |
| Secure Boot | enabled，lockdown=integrity，原MOK保留；仍用efi=noruntime |
| Wi-Fi | connected，hard block修复，UEFI推导MAC 78:86:2e:5e:ec:47 |
| 蓝牙 | 服务正常，鼠标自动重连已退役，保留蓝牙本身 |
| 触控板 | iptsd 3.1.0+sl7.2-1，物理按钮/触点状态修复；最终校准保留，轻触点击开启 |
| 触摸屏 | SPI10/a88000，045E:0C6F，用户确认实际触摸工作；对应iptsd服务正常 |
| 默认启动 | sl7-spi-touchscreen-test，菜单名SL7 - Wi-Fi, touchpad and touchscreen |
| 回退 | 原sl7-combined-kernel及官方Ubuntu均保留，5秒菜单 |

启动项和镜像路径中的test沿用之前的名字，现已由用户批准设为默认。实际镜像/内核/initrd未被这次默认切换改写，签名验证通过。不需要再次登记MOK。

## 今天新增的修复和验收

1. 对照Windows真实PnP/ACPI，改用MSHW0461/GTCH SPI触摸屏。旧I2C参考对应另一硬件分支，失败试验作为历史保留。新设备树使用SPI10、GPIO51中断、GPIO48复位、GPIO64供电；复用已验证内核代码及模块，独立签名镜像现已实机成功。
2. 定位重复点击：原版在不稳定触点/主触点切换时产生假离开，还会提前释放物理按钮且内部状态不同步。修复后保持多点槽与触点计数一致，按钮按硬件状态释放；禁用/手掌屏蔽时同步清理状态。46项离线检查通过，物理单击、长按、轻触三轮均10次click/0双击。
3. 按用户要求正式使用点击修复。最终包3.1.0+sl7.2-1与测试版使用相同二进制；额外修正升级时重复触发hidraw探测的问题。首次升级后该探测导致触控板systemd设备标签丢失、服务停止；已针对该设备恢复标签，升级脚本现仅首次安装触发探测。最终两服务持续active、重启计数0，虚拟触摸屏和触控板均存在。
4. 默认已切换到成功的触摸屏镜像，保留原正常组合镜像与官方内核。最终状态证据在validation/day-final-state.json。

## 已知问题，留待以后

快速连续物理点击时仍可能短暂停顿，旧版和修复版均发生过。三批SPI跟踪共40722条消息均有完成事件，未发现遗留请求或跟踪丢事件。标记卡住时部分数据流仍持续，但468字节热图消失，flags出现0x08及未解码type0x12；恢复后热图回来。不能把它说成已经修复、SPI必然挂死、过流或噪声保护；字段含义尚待确认。日志及Windows对照交接在experimental/touchpad-click。

触摸屏本轮保持供电开启，睡眠恢复、唤醒和完整功耗管理尚未验收。没有在本次收尾中扩大这部分范围。

## 维护与恢复

- patchsets/kernel/series：3个补丁，Wi-Fi、QSPI触控板、GTCH SPI触摸屏。
- patchsets/iptsd/series：上游v3.1.0到已用fork的完整差异，再加本地触点/按钮修复。
- patchsets/iptsd-packaging/：避免升级时重新探测正在初始化的设备。
- config/system-root/：最终服务、校准、地址和启动配置快照。
- sources/iptsd-sl7-click-fixed-source.tar.gz：最终iptsd源码；旧快照保留用于溯源。
- 恢复包包含原内核包、当前iptsd包、旧iptsd回退包、当前已签名触屏镜像、匹配initrd及原始构建输入。

源码补丁在原Ubuntu源码/iptsd上游上零fuzz重放通过；iptsd全部147个源码文件与实际构建输入一致。每个归档含SHA256SUMS和FILE-MANIFEST.json，压缩包逐文件流式校验。归档不含签名私钥、MOK口令或网络认证资料。
